<UserControl x:Class="Arkivverket.Arkade.GUI.Views.CreatePackage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:resources="clr-namespace:Arkivverket.Arkade.GUI.Resources"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:Arkivverket.Arkade.GUI.Models"
             xmlns:validation="clr-namespace:Arkivverket.Arkade.GUI.Util"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="600" Width="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="218" />
            <ColumnDefinition Width="23" />
            <ColumnDefinition Width="218" />
            <ColumnDefinition Width="23" />
            <ColumnDefinition Width="218" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="6"
                   Text="{x:Static resources:GUI.CreatePackageWindowTitle}"
                   Style="{StaticResource MaterialDesignTitleTextBlock}"
                   Margin="10" />

        
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Grid.ColumnSpan="6"
                   Margin="10"
                   Style="{StaticResource MaterialDesignSubheadingTextBlock}"
                   Text="{x:Static resources:MetaDataGUI.MetadataRequiredFieldsInfo}" />
        
        <Button Grid.Row="1" Grid.Column="5" Height="24" Width="190"
                Content="{x:StaticExtension resources:MetaDataGUI.LoadExternalMetadataButtonText}"
                ToolTip="{x:StaticExtension resources:MetaDataGUI.LoadExternalMetadataInfoText}"
                Command="{Binding LoadExternalMetadataCommand}" />

        <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~ METADATA ~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->
        <ScrollViewer Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="6" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled" Height="Auto" Margin="20,25">

            <StackPanel>

                <!-- Arkivbeskrivelse -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_ArchiveMain}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1" BorderThickness="0">
                </GroupBox>

                <Border BorderBrush="LightGray" BorderThickness="1"
                        CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataModelArchiveDescription.VisibilityItem}"
                        Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveDescription}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataModelArchiveDescription.ArchiveDescription, Mode=TwoWay}"
                                 TextWrapping="Wrap" />


                        <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.AgreementNumber}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataModelArchiveDescription.AgreementNumber" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                    </Grid>
                </Border>


                <!-- Arkivskaper -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_ArchiveCreator}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding AddMetadataAchiveCreatorEntry}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button>
                    </StackPanel>
                </GroupBox>

                <ItemsControl ItemsSource="{Binding MetaDataArchiveCreators}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="5" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>

                        <DataTemplate DataType="{x:Type models:GuiMetaDataModel}">
                            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                                    Visibility="{Binding VisibilityItem}">
                                <Grid Margin="5">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="250" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,0,15"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveCreator}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                                        <TextBox.Text>
                                            <Binding Path="Entity" Mode="TwoWay">
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>

                                    <TextBox Grid.Row="1" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveCreator_ContactPerson}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding ContactPerson, Mode=TwoWay}" />

                                    <TextBox Grid.Row="2" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveCreator_Address}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Address, Mode=TwoWay}" />
                                    
                                    <TextBox Grid.Row="3" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveCreator_Telephone}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Telephone, Mode=TwoWay}" />

                                    <TextBox Grid.Row="4" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveCreator_Email}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Email, Mode=TwoWay}" />

                                    <Button Grid.Row="5"
                                            Style="{DynamicResource MaterialDesignToolButton}"
                                            Height="24" Width="24" Command="{Binding CommandDeleteItem}"
                                            Visibility="{Binding DeleteButtonVisibility}" HorizontalAlignment="Right">
                                        <materialDesign:PackIcon Kind="{Binding IconDelete}" Opacity=".56" />
                                    </Button>


                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>


                <!-- Overforer -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_Transferer}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <!--<Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding AddMetadataAchiveTransfererEntry}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button> -->

                    </StackPanel>
                </GroupBox>

                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataTransferer.ItemVisibility}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Transferer}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataTransferer.Entity" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Transferer_ContractPerson}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataTransferer.ContactPerson" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>


                        <TextBox Grid.Row="2" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Transferer_Address}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataTransferer.Address, Mode=TwoWay}" />
                        
                        <TextBox Grid.Row="3" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Transferer_Telephone}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataTransferer.Telephone, Mode=TwoWay}" />

                        <TextBox Grid.Row="4" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Transferer_Email}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataTransferer.Email, Mode=TwoWay}" />

                        <Button Grid.Row="5" HorizontalAlignment="Right"
                                Style="{DynamicResource MaterialDesignToolButton}"
                                Height="24" Width="24" Command="{Binding MetaDataTransferer.CommandNullOutEntry}">
                            <materialDesign:PackIcon Kind="{Binding MetaDataTransferer.IconDelete}" Opacity=".56" />
                        </Button>
                    </Grid>
                </Border>


                <!-- Produsent -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_Producer}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <!--<Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding AddMetadataAchiveProducerEntry}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button> -->

                    </StackPanel>
                </GroupBox>


                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding ItemVisibility}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Producer}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataProducer.Entity" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>


                        <TextBox Grid.Row="1" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Producer_ContactPerson}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataProducer.ContactPerson" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBox Grid.Row="2"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Producer_Address}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataProducer.Address, Mode=TwoWay}" />

                        <TextBox Grid.Row="3"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Producer_Telephone}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataProducer.Telephone, Mode=TwoWay}" />

                        <TextBox Grid.Row="4"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Producer_Email}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataProducer.Email, Mode=TwoWay}" />

                        <Button Grid.Row="5" HorizontalAlignment="Right"
                                Style="{DynamicResource MaterialDesignToolButton}"
                                Height="24" Width="24" Command="{Binding MetaDataProducer.CommandNullOutEntry}">
                            <materialDesign:PackIcon Kind="{Binding MetaDataProducer.IconDelete}" Opacity=".56" />
                        </Button>
                    </Grid>
                </Border>


                <!-- Eier -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_Owner}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding AddMetadataAchiveOwnerEntry}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button>

                    </StackPanel>
                </GroupBox>

                <ItemsControl ItemsSource="{Binding MetaDataOwners}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="5" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>

                        <DataTemplate DataType="{x:Type models:GuiMetaDataModel}">
                            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                                    Visibility="{Binding VisibilityItem}">
                                <Grid Margin="5">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="250" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,0,15"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Owner}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                                        <TextBox.Text>
                                            <Binding Path="Entity" Mode="TwoWay">
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>

                                    <TextBox Grid.Row="1" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Owner_ContactPerson}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding ContactPerson, Mode=TwoWay}" />

                                    <TextBox Grid.Row="2" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Owner_Address}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Address, Mode=TwoWay}" />

                                    <TextBox Grid.Row="3" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Owner_Telephone}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Telephone, Mode=TwoWay}" />

                                    <TextBox Grid.Row="4" Grid.Column="0"
                                             materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Owner_Email}"
                                             Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                             Text="{Binding Email, Mode=TwoWay}" />

                                    <Button Grid.Row="5"
                                            Style="{DynamicResource MaterialDesignToolButton}"
                                            Height="24" Width="24" Command="{Binding CommandDeleteItem}"
                                            Visibility="{Binding DeleteButtonVisibility}" HorizontalAlignment="Right">
                                        <materialDesign:PackIcon Kind="{Binding IconDelete}" Opacity=".56" />
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Pakkeskaper -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_Creator}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">

                    <StackPanel Orientation="Horizontal">
                        <Border BorderBrush="LightGray" BorderThickness="1"
                        CornerRadius="10" Margin="5" Width="269"
                        HorizontalAlignment="Left">
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="250" />
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Creator}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                                    <TextBox.Text>
                                        <Binding Path="MetaDataCreator.Entity" Mode="TwoWay">
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>

                                <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Creator_ContactPerson}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                                    <TextBox.Text>
                                        <Binding Path="MetaDataCreator.ContactPerson" Mode="TwoWay">
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>

                                <TextBox Grid.Row="2" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Creator_Address}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataCreator.Address, Mode=TwoWay}" />

                                <TextBox Grid.Row="3" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Creator_Telephone}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataCreator.Telephone, Mode=TwoWay}" />

                                <TextBox Grid.Row="4" Grid.Column="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Creator_Email}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataCreator.Email, Mode=TwoWay}" />
                            </Grid>
                        </Border>
                        <!-- Pakkeskaper-system -->
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                                MinWidth="200" Height="Auto" VerticalAlignment="Bottom">
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Margin="5">
                                    <TextBlock Text="{x:Static resources:MetaDataGUI.Creator_System}" FontSize="12" Margin="0, 5" /><LineBreak />
                                    <TextBlock Text="{Binding Path=ArkadeNameAndCurrentVersion}" />
                                </TextBlock>

                                <TextBlock Grid.Row="1" Margin="5" Visibility="{Binding Path=MetaDataCreatorSoftwareSystem.SystemName, TargetNullValue=Collapsed}">
                                    <TextBlock Text="{x:Static resources:MetaDataGUI.Creator_OriginalSystem}" FontSize="12" Margin="0, 5" /><LineBreak />
                                    <TextBlock Text="{Binding MetaDataCreatorSoftwareSystem.SystemName}" />
                                    <TextBlock Text="{Binding MetaDataCreatorSoftwareSystem.SystemVersion}" />
                                </TextBlock>
                            </Grid>
                        </Border>
                    </StackPanel>
                </GroupBox>

                <!-- Mottaker -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_Recipient}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1" BorderThickness="0" Margin="0,20,0,0" />

                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataRecipient.ItemVisibility}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Recepient}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataRecipient.Entity" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Grid>
                </Border>


                <!-- System -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_System}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0"/>

                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataSystem.ItemVisibility}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,15"
                                 Name="MetaDataSystemName"
                                 GotFocus="UpdateStandardLabel" TextChanged="UpdateStandardLabel"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemName}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataSystem.SystemName" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>


                        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,15"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemVersion}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.Text>
                                <Binding Path="MetaDataSystem.SystemVersion" Mode="TwoWay">
                                </Binding>
                            </TextBox.Text>
                        </TextBox>


                        <ComboBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                  materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemType}" 
                                  MinWidth="72"
                                  ItemsSource="{Binding SystemTypeList}"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                            <ComboBox.Text>
                                <Binding Path="MetaDataSystem.SystemType" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                </Binding>
                            </ComboBox.Text>
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>


                        <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemTypeVersion}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataSystem.SystemTypeVersion, Mode=TwoWay}" />

                    </Grid>
                </Border>





                <!-- Uttrekkssystem -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_ArchiveSystem}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding MetaDataArchiveSystem.CommandAddItem}"
                                Visibility="{Binding MetaDataArchiveSystem.VisibilityAddItem}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button>
                    </StackPanel>
                </GroupBox>

                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataArchiveSystem.VisibilityItem}" Width="269"
                        HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Row="0"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ArchiveSystem}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataArchiveSystem.SystemName, Mode=TwoWay}" />

                        <TextBox Grid.Row="1"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemVersion}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataArchiveSystem.SystemVersion, Mode=TwoWay}" />


                        <ComboBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                  materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemType}" 
                                  MinWidth="72"
                                  ItemsSource="{Binding SystemTypeList}"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                            <ComboBox.Text>
                                <Binding Path="MetaDataArchiveSystem.SystemType" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                </Binding>
                            </ComboBox.Text>
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>


                        <TextBox Grid.Row="3"
                                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.SystemTypeVersion}"
                                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                 Text="{Binding MetaDataArchiveSystem.SystemTypeVersion, Mode=TwoWay}" />

                        <Button Grid.Row="4"
                                Style="{DynamicResource MaterialDesignToolButton}" HorizontalAlignment="Right"
                                Height="24" Width="24" Command="{Binding  MetaDataArchiveSystem.CommandDeleteItem}">
                            <materialDesign:PackIcon Kind="{Binding  MetaDataArchiveSystem.IconDelete}" Opacity=".56" />
                        </Button>
                    </Grid>
                </Border>





                <!-- Noark section (start/end/inncomming/outgoing seps) -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_NoarkParameters}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{DynamicResource MaterialDesignToolButton}"
                                Height="32" Width="32"
                                Command="{Binding MetaDataNoarkSection.CommandAddItem}"
                                Visibility="{Binding MetaDataNoarkSection.VisibilityAddItem}">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Opacity=".85" />
                        </Button>
                    </StackPanel>
                </GroupBox>


                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataNoarkSection.VisibilityItem}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>


                        <DatePicker Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                                    Name="MetadataStartDate"
                                    GotFocus="UpdateStandardLabel" SelectedDateChanged="UpdateStandardLabel"
                                    Width="250" HorizontalAlignment="Left" Margin="0 16 0 0" 
                                    Language="no"
                                    materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.StartDate}" 
                                    Style="{StaticResource MaterialDesignFloatingHintDatePicker}" 
                                    SelectedDate="{Binding MetaDataNoarkSection.StartDate, Mode=TwoWay}" CalendarOpened="DatePicker_CalendarOpened"/>

                        <DatePicker Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                    Name="MetadataEndDate"
                                    GotFocus="UpdateStandardLabel" SelectedDateChanged="UpdateStandardLabel"
                                    Width="250" HorizontalAlignment="Left" Margin="0 16 0 0" 
                                    Language="no"
                                    materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.EndData}" 
                                    Style="{StaticResource MaterialDesignFloatingHintDatePicker}" 
                                    SelectedDate="{Binding MetaDataNoarkSection.EndDate, Mode=TwoWay}" CalendarOpened="DatePicker_CalendarOpened"/>

                        <Button Grid.Row="4" Grid.Column="0" HorizontalAlignment="Right"
                                Style="{DynamicResource MaterialDesignToolButton}"
                                Height="24" Width="24" Command="{Binding MetaDataNoarkSection.CommandDeleteItem}">
                            <materialDesign:PackIcon Kind="{Binding MetaDataNoarkSection.IconDelete}" Opacity=".56" />
                        </Button>
                    </Grid>
                </Border>




                <!-- Uttrekksdato -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.GuiSubsection_ExtractionDate}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                    </StackPanel>
                </GroupBox>


                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10" Margin="5"
                        Visibility="{Binding MetaDataExtractionDate.VisibilityItem}" Width="269" HorizontalAlignment="Left">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <DatePicker Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                                    Width="250" HorizontalAlignment="Left" Margin="0 16 0 0" 
                                    Language="no"
                                    materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.ExtractionDate}" 
                                    Style="{StaticResource MaterialDesignFloatingHintDatePicker}" 
                                    SelectedDate="{Binding MetaDataExtractionDate.ExtractionDate, Mode=TwoWay}" CalendarOpened="DatePicker_CalendarOpened"/>
                    </Grid>
                </Border>

                <!-- Label -->
                <GroupBox Header="{x:Static resources:MetaDataGUI.Label}"
                          Style="{DynamicResource MaterialDesignGroupBox}"
                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                          BorderThickness="0" Margin="0,20,0,0">
                </GroupBox>

                <StackPanel Orientation="Horizontal" Margin="5 5 0 0">
                    <RadioButton Content="{x:Static resources:MetaDataGUI.Label_OptionTextStandardLabel}"
                                 IsChecked="{Binding StandardLabelIsSelected}"
                                 Style="{StaticResource MaterialDesignRadioButton}"/>
                    <RadioButton Content="{x:Static resources:MetaDataGUI.Label_OptionTextUserdefinedLabel}"
                                 IsChecked="{Binding UserdefinedLabelIsSelected}"
                                 Style="{StaticResource MaterialDesignRadioButton}"
                                 Margin="205 0 0 0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10"
                            Margin="5" Width="275" HorizontalAlignment="Left"
                            Visibility="{Binding MetaDataExtractionDate.VisibilityItem}">
                        <Grid Margin="5">
                            <TextBox Margin="0,0,0,5" Width="260" TextWrapping="Wrap"
                                     Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Label_HintTextStandardLabel}"
                                     Text="{Binding MetaDataNoarkSection.StandardLabel}"
                                     IsEnabled="{Binding StandardLabelIsSelected}"
                                     IsReadOnly="True" />
                        </Grid>
                    </Border>
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10"
                            Margin="5" Width="275" HorizontalAlignment="Left"
                            Visibility="{Binding MetaDataExtractionDate.VisibilityItem}">
                        <Grid Margin="5">
                            <TextBox Margin="0,0,0,5" Width="260" TextWrapping="Wrap"
                                     Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                                     materialDesign:HintAssist.Hint="{x:Static resources:MetaDataGUI.Label_HintTextUserdefinedLabel}"
                                     Text="{Binding MetaDataNoarkSection.UserdefinedLabel, Mode=TwoWay}"
                                     IsEnabled="{Binding UserdefinedLabelIsSelected}" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- END METADATA  -->

                <!-- Line divider  -->
                <Border Margin="0,16" BorderThickness="0 2 0 0" BorderBrush="{DynamicResource MaterialDesignDivider}" />

                <!-- Create SIP/AIP package  -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="{x:Static resources:GUI.CreatePackageSelectTypeLabel}" Margin="10" />

                <StackPanel Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Margin="10" Orientation="Horizontal">
                    <RadioButton Content="{x:Static resources:GUI.CreatePackageTypeSIP}"
                         IsChecked="{Binding SelectedPackageTypeSip, Mode=TwoWay}"
                         Style="{StaticResource MaterialDesignRadioButton}" Margin="0 0 8 8" VerticalAlignment="Center" />
                    <RadioButton Content="{x:Static resources:GUI.CreatePackageTypeAIP}"
                         IsChecked="{Binding SelectedPackageTypeAip, Mode=TwoWay}"
                         Style="{StaticResource MaterialDesignRadioButton}" Margin="0 0 8 8" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" Margin="10 10" Orientation="Horizontal">
                    <Button
                Command="{Binding CreatePackageCommand}"
                Content="{x:Static resources:GUI.CreatePackageButtonText}" HorizontalAlignment="Left" Width="130"
                Height="30" />

                    <Button
                Command="{Binding NewProgramSessionCommand}"
                Content="{x:Static resources:GUI.NewProgramSessionButton}" HorizontalAlignment="Left" Width="130"
                Height="30" Margin="5,0,0,0" />
                </StackPanel>
                <StackPanel Grid.Row="8"  Grid.Column="0" Grid.ColumnSpan="3" Margin="10" Orientation="Horizontal">
                    <ProgressBar Visibility="{Binding ProgressBarVisibility}" Style="{StaticResource MaterialDesignCircularProgressBar}"                     
                                 Value="0"
                                 IsIndeterminate="True"
                                 Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12 12 12 12" Width="19"/>
                    <TextBlock Visibility="{Binding ProgressBarVisibility}"  Text="{x:Static resources:MetaDataGUI.PackageCreationOngoing}" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="5" Margin="10" Orientation="Vertical">
                    <TextBlock Text="{Binding StatusMessageText, Mode=OneWay}" TextWrapping="Wrap" />
                    <TextBox Background="Transparent" BorderThickness="0" Text="{Binding StatusMessagePath}" IsReadOnly="True"
                     TextWrapping="Wrap" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>