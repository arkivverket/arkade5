<UserControl x:Class="Arkivverket.Arkade.UI.Views.CreatePackage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:resources="clr-namespace:Arkivverket.Arkade.UI.Resources"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="730">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="218"/>
            <ColumnDefinition Width="23"/>
            <ColumnDefinition Width="218"/>
            <ColumnDefinition Width="23"/>
            <ColumnDefinition Width="218"/>
            <ColumnDefinition Width="23"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Grid.ColumnSpan="2" 
                   Text="{x:Static resources:UI.CreatePackageWindowTitle}" 
                   Style="{StaticResource MaterialDesignTitleTextBlock}" 
                   Margin="10"/>

        <TextBlock Grid.Row="1" 
                   Grid.Column="0" 
                   Grid.ColumnSpan="3"
                   Margin="10" 
                   Style="{StaticResource MaterialDesignSubheadingTextBlock}"
                   Text="{x:Static resources:MetaDataUI.Title}"/>


        <!-- Arkiv -->
        <!--
        <GroupBox Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_ArchiveMain}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="3" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.ArchiveDescription}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="4" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.AgreementNumber}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />
        -->

        <!-- Arkivskaper -->
        <!--
        <GroupBox Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_ArchiveCreator}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="3" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.ArchiveCreator}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="4" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.ArchiveCreator_ContactPerson}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="5" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.ArchiveCreator_Telephone}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="6" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.ArchiveCreator_Email}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        -->

        <!-- Overforer -->
        <!--
        <GroupBox Grid.Column="4" Grid.Row="2" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_Transferer}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="3" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Transferer}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="4" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Transferer_ContractPerson}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="5" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Transferer_Telephone}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="6" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Transferer_Email}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        -->

        <!-- Produsent -->
        
        <!--
        <GroupBox Grid.Column="0" Grid.Row="8" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_Producer}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="9" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Producer}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="10" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Producer_ContactPerson}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="11" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Producer_Telephone}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="12" 
                 Grid.Column="0"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Producer_Email}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />
        -->

        <!-- Eier -->
        <!--
        <GroupBox Grid.Column="2" Grid.Row="8" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_Owner}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="9" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Owner}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="10" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Owner_ContactPerson}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="11" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Owner_Telephone}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="12" 
                 Grid.Column="2"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.Owner_Email}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        -->

        <!-- System -->
        <!--
        <GroupBox Grid.Column="4" Grid.Row="8" Grid.ColumnSpan="2"  
                  Header="{x:Static resources:MetaDataUI.GuiSubsection_System}" 
                  Style="{DynamicResource MaterialDesignGroupBox}" Margin="10 5 5 0" materialDesign:ShadowAssist.ShadowDepth="Depth2" BorderThickness="0">
        </GroupBox>

        <TextBox Grid.Row="9" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.SystemName}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="10" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.SystemVersion}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="11" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.SystemType}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        <TextBox Grid.Row="12" 
                 Grid.Column="4"
                 Margin="20 5 5 5"
                 materialDesign:HintAssist.Hint="{x:Static resources:MetaDataUI.SystemTypeVersion}" 
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" />

        -->

        <TextBlock Grid.Row="15
                   " Grid.Column="0" Text="{x:Static resources:UI.CreatePackageSelectTypeLabel}" Margin="10"/>
        
        <StackPanel Grid.Row="15" Grid.Column="1" Grid.ColumnSpan="2" Margin="10" Orientation="Horizontal">
            <RadioButton Content="{x:Static resources:UI.CreatePackageTypeSIP}" 
                         IsChecked="{Binding SelectedPackageTypeSip, Mode=TwoWay}"  
                         Style="{StaticResource MaterialDesignRadioButton}" Margin="0 0 8 8" VerticalAlignment="Center" />
            <RadioButton Content="{x:Static resources:UI.CreatePackageTypeAIP}"
                         IsChecked="{Binding SelectedPackageTypeAip, Mode=TwoWay}" 
                         Style="{StaticResource MaterialDesignRadioButton}" Margin="0 0 8 8" VerticalAlignment="Center" />
        </StackPanel>
        
        <StackPanel Grid.Row="16" Grid.Column="0" Grid.ColumnSpan="3" Margin="10 10" Orientation="Horizontal">
            <Button  
                Command="{Binding CreatePackageCommand}"
                Content="{x:Static resources:UI.CreatePackageButtonText}" HorizontalAlignment="Left" Width="130" Height="30" />

            <Button  
                Command="{Binding NewProgramSessionCommand}"
                Content="{x:Static resources:UI.NewProgramSessionButton}" HorizontalAlignment="Left" Width="130" Height="30" Margin="5,0,0,0"/>
        </StackPanel>

        <StackPanel  Grid.Row="17" Grid.Column="0" Grid.ColumnSpan="5" Margin="10" Orientation="Vertical">
            <TextBlock Text="{Binding StatusMessageText, Mode=OneWay}"  TextWrapping="Wrap" />
            <TextBox Background="Transparent" BorderThickness="0" Text="{Binding StatusMessagePath}" IsReadOnly="True" TextWrapping="Wrap" />
        </StackPanel>
        
        

    </Grid>
</UserControl>
